spring:
  datasource:
    url: jdbc:h2:mem:test2
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
    properties:
      hibernate: